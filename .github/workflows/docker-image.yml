name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: cd kinetic/docker && docker build . --tag kmr/env-kinetic:$(date +%s) --tag kmr/env-kinetic:latest
